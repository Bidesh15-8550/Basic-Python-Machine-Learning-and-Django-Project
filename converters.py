#imported on modules.py
def lbs_to_kg(weight):
    return weight*.45

def kg_to_lbs(weight):
    return weight/.45




from utils import find_max
numbers = [10,3,6,2]
maximum = find_max(numbers)

print(maximum(numbers))